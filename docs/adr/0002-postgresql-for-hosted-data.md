# Use PostgreSQL for hosted data

faðir will store hosted data in one PostgreSQL database instead of one database for each User. Workspace and Portfolio scopes will separate private rows. Market reference data can remain shared.
