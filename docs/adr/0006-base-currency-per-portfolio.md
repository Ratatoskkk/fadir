# Select a Base Currency for each Portfolio

Each Portfolio will select and change its own Base Currency instead of using TRY for all views. Transactions will remain in their native currencies. A Base Currency change will revalue views without a transaction rewrite.
