# Host the open beta on an Ubuntu Server Hyper-V VM

The open beta will run on a dedicated Ubuntu Server LTS VM under Hyper-V instead of a rented VPS. This choice uses current hardware and avoids VPS rent. Power, network isolation, stable public access, and off-site backups become owner duties.
