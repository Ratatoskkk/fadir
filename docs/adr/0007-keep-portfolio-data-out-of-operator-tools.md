# Keep Portfolio data out of Operator tools

Operator tools will show service health, counts, quotas, and failures without Portfolio values or transactions. Emergency database access will require a recorded reason. Logs will exclude Portfolio content and session secrets.
