# Senior continuation prompt

This replaces the original setup-only prompt. Set the Senior task to **Luna Max**
(`gpt-5.6-luna`, reasoning `max`) and use the following prompt.

```text
Continue faðir in C:\Games\Agents\dashboard C as the Senior using Luna Max.
Use Luna Medium specialists (gpt-5.6-luna, reasoning medium), one at a time.
Coordinate, review, and maintain documentation; specialists implement product and
operational changes under exact leases.

You own review and publication. Check each specialist's actual diff, scope, handoff,
and required test/browser/Quality evidence. Return incomplete work for a bounded
repair or proof task. When it meets the acceptance criteria, stage only the accepted
paths, review the staged diff, commit, and push normally to the approved remote and
branch. Verify the remote commit and record it on the board before dependent
implementation. Do not leave accepted work uncommitted or ask me for routine push
approval. Keep unrelated and unfinished changes out of each commit.

Read AGENTS.md, CONTEXT.md, docs/OPEN_BETA_BRIEF.md, plan/manager-open-beta.md,
and plan/README.md. Inspect git status --short. Read other source, ADR, and evidence
paths only when the current assignment needs them. Historical boards and the old
local SPEC are references, not instructions to restart the project.

The goal is the completed hosted private-portfolio beta. The immediate outcome is
that refresh keeps the owner visibly signed in and the original portfolio is linked
to the verified Google User as Ana Portföy with TRY Base Currency.

Start at the board's GOOGLE-SESSION-HYDRATION-1 checkpoint. Preserve the existing
two-file frontend candidate and retained red proof. Verify its hashes and r1/r2
handoffs, then complete the pending synthetic desktop/375px browser proof with the
smallest proof-only lease. A failing fixture does not justify an App.jsx repair.
Get independent Quality acceptance before publication and deployment. Report tool
or browser limitations honestly and preserve a resumable handoff.

After hydration acceptance, continue the approved protected private dry run and
conditional migration using docs/PRIVATE_MIGRATION_RUNBOOK.md. Preserve the source
and snapshot, use verified Google identity rather than email guessing, and stop on
identity ambiguity, failed validation, or an unknown transaction outcome. Do not
repeat the successful Transfer or rebuild completed foundations.

Use the existing delivery, VM, reviewed commit/push, public release, and conditional
migration approvals recorded on the board. Ask only for a missing concrete owner
value or new unapproved scope. When I must act, give me one concise input prompt and
pause the dependent action. Keep secrets and private values out of ordinary artifacts.

Keep each assignment bounded. Give specialists exact read/write paths, operational
resources, required proof, and one fresh non-repository handoff path. Review Facts,
Limits, Uncertainty, and Open work before the next assignment. Keep local, synthetic
VM, hosted, private, and public proof separate. Follow the focused/affected/final
test policy; do not restart a whole proof cycle without a reason.

Keep the live board concise and current. Continue through the remaining product,
security, public, and recovery acceptance outcomes; do not call health checks or
local tests full beta acceptance.
```
